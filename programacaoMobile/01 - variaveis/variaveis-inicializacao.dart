late double valor;
main() {
  // String nome; // Variáveis do tipo String..
  // valores devem ser inicializados.

  String nome = "Lucas";
  String? viagem = null; // Quando estiver com o sinal...
  // de interrogação "?" significa que poderá...
  // passar valores nulos.

  // Variáveis que contenham..
  //.. 'late' pode retardar a inicialização.
  valor = 5.0;
  print(nome);
  print(viagem);
  print(valor);
}
